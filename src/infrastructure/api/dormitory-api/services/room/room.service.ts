import { injectable } from 'inversify';

@injectable()
export class RoomService {
  // constructor(
  // @inject(DormitoryApiHttpService)
  // private readonly httpService: DormitoryApiHttpService
  // ) {
  // async getRoomsWithFilters() {
  // 	try {
  // 		const response = await this.httpService.instance.get('/rooms')
  // 	} catch {
  //
  // 	}
  // }
  // }
}
